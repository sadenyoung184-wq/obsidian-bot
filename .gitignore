# فایل محیطی — هرگز کامیت نشود (توکن‌ها داخلش است)
.env

# پایتون
__pycache__/
*.py[cod]
*.log
.venv/
venv/

# والت محلی هاست (داخل گیت نمی‌آید، فقط روی هاست ساخته می‌شود)
MyVault/

# فایل‌های وضعیت محلی ربات (ایندکس حافظه، پیش‌نویس‌ها و یادآورهای تستی — روی هاست بازسازی می‌شوند)
memory.json
drafts.json
Reminders/
